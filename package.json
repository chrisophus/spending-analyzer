{
  "name": "spending-analyzer",
  "version": "0.1.0",
  "description": "Read-only CLI that turns bank/credit-card CSV exports into a spending report: totals, discretionary vs. essential, and a recurring-charge / subscription audit. No Actual Budget, no server.",
  "type": "module",
  "bin": {
    "spending-analyzer": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.37.0",
    "chalk": "^5.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
